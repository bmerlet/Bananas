<Window x:Class="Dashboard.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Investment dashboard" Height="450" Width="800">
    <Window.InputBindings>
        <KeyBinding Key="F5" Command="{Binding RefreshCommand}"/>
    </Window.InputBindings>
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" HorizontalAlignment="Stretch">
            <Menu HorizontalAlignment="Stretch">
                <MenuItem Header="_File">
                    <MenuItem Header="_Info"   Command="{Binding InfoCommand}"/>
                    <MenuItem Header="Refresh" Command="{Binding RefreshCommand}" InputGestureText="F5"/>
                    <MenuItem Header="_Exit"   Command="{Binding ExitCommand}"/>
                </MenuItem>
            </Menu>
        </StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="97*"/>
            </Grid.ColumnDefinitions>

        </Grid>
    </DockPanel>
</Window>
