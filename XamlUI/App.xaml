<Application x:Class="XamlUI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:XamlUI"
             xmlns:w="clr-namespace:XamlUI.Widgets"
             xmlns:t="clr-namespace:XamlUI.Tools"
             StartupUri="MainWindow.xaml">
    <Application.Resources>

        <!--
          == Converters ==
          -->
        <t:VisibilityBoolConverter x:Key="visibilityBoolConverter"/>
        <t:FontWeightConverter x:Key="fontWeightConverter"/>
        <t:TransactionStateToBrushConverter x:Key="transactionStateToBrushConverter"/>
        <t:AmountToStringConverter x:Key="amountToStringConverter"/>
        <t:AmountToStringNoNegativeConverter x:Key="amountToStringNoNegativeConverter"/>
        <t:AmountToColorConverter x:Key="amountToColorConverter"/>
        <t:ReconcileAmountMultiConverter x:Key="reconcileAmountMultiConverter"/>
        <t:PercentTostringConverter x:Key="percentTostringConverter"/>
        <t:GridLengthConverter x:Key="gridLengthConverter"/>


        <!-- Default for amount box -->
        <Style TargetType="w:AmountBox">
            <Setter Property="HorizontalContentAlignment" Value="Right"/>
        </Style>

        <!--
          == Styles for form-like dialogs ==
          -->

        <!-- Form text blocks -->
        <Style TargetType="TextBlock" x:Key="FormTextBlock">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Form combo box -->
        <Style TargetType="ComboBox" x:Key="FormComboBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Form check box -->
        <Style TargetType="CheckBox" x:Key="FormCheckBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Form watermark -->
        <Style TargetType="w:WatermarkTextBox" x:Key="FormWatermarkTextBox">
            <Setter Property="Margin" Value="5"/>
        </Style>

        <!-- Form amount box -->
        <Style TargetType="w:AmountBox" x:Key="FormAmountBox">
            <Setter Property="ShowZeroAmount" Value="True"/>
            <Setter Property="HorizontalContentAlignment" Value="Right"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
        
        <!-- Form date box -->
        <Style TargetType="w:DateBox" x:Key="FormDateBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="HorizontalContentAlignment" Value="Right"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- Form text box -->
        <Style TargetType="TextBox" x:Key="FormTextBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Form button -->
        <Style TargetType="Button" x:Key="FormButton">
            <Setter Property="Margin" Value="5"/>
        </Style>

        <!-- Form radio button -->
        <Style TargetType="RadioButton" x:Key="FormRadioButton">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Form autocomplete-->
        <Style TargetType="w:AutoCompleteTextBox" x:Key="FormAutocompleteTextBox">
            <Setter Property="Margin" Value="0"/>
        </Style>

        <!--
          == Buttons ==
          -->

        <!-- OK button -->
        <Style TargetType="Button" x:Key="OKButton">
            <Setter Property="Content" Value="OK"/>
            <Setter Property="IsDefault" Value="True"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property ="VerticalAlignment" Value="Center"/>
            <Setter Property="Width" Value="75" />
            <Setter Property="Margin" Value="0,0,10,0" />
        </Style>

        <!-- Cancel button -->
        <Style TargetType="Button" x:Key="CancelButton">
            <Setter Property="Content" Value="Cancel"/>
            <Setter Property="IsCancel" Value="True"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property ="VerticalAlignment" Value="Center"/>
            <Setter Property="Width" Value="75" />
            <Setter Property="Margin" Value="0,0,10,0" />
        </Style>

        <!-- Done button -->
        <Style TargetType="Button" x:Key="DoneButton">
            <Setter Property="Content" Value="Done"/>
            <Setter Property="IsDefault" Value="True"/>
            <Setter Property="IsCancel" Value="True"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property ="VerticalAlignment" Value="Center"/>
            <Setter Property="Width" Value="75" />
            <Setter Property="Margin" Value="0,0,10,0" />
        </Style>

        <!-- Style for category autocomplete textbox -->
        <Style TargetType="{x:Type w:AutoCompleteTextBox}" x:Key="CategoryBoxStyle">
            <Setter Property="Watermark" Value="Category"/>
            <Setter Property="DisplayMember" Value="FullName"/>
            <Setter Property="ListBoxHeight" Value="150"/>
            <Setter Property="ListBoxWidth" Value="200"/>
            <Setter Property="IsTextFromItemsSourceOnly" Value="True"/>
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding FullName}" FontWeight="{Binding FontWeight, Converter={StaticResource fontWeightConverter}}" />
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style for category autocomplete textbox -->
        <Style TargetType="{x:Type w:AutoCompleteTextBox}" x:Key="AccountBoxStyle">
            <Setter Property="Watermark" Value="Account"/>
            <Setter Property="DisplayMember" Value="Name"/>
            <Setter Property="ListBoxHeight" Value="150"/>
            <Setter Property="ListBoxWidth" Value="200"/>
            <Setter Property="IsTextFromItemsSourceOnly" Value="True"/>
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style for payee autocomplete textbox-->
        <Style TargetType="{x:Type w:AutoCompleteTextBox}" x:Key="PayeeBoxStyle">
            <Setter Property="Watermark" Value="Payee"/>
            <Setter Property="DisplayMember" Value="Payee"/>
            <Setter Property="ListBoxHeight" Value="150"/>
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="65"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="100"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Payee}"/>
                            <TextBlock 
                                Text="{Binding Amount, Converter={StaticResource amountToStringConverter}}"
                                Foreground="{Binding Amount, Converter={StaticResource amountToColorConverter}}"
                                Grid.Column="1" Margin="3,0" HorizontalAlignment="Right"/>
                            <TextBlock Text="{Binding Category}" Grid.Column="2" Foreground="Blue"/>
                            <TextBlock Text="{Binding Memo}"     Grid.Column="3" Margin="3,0,0,0"/>
                        </Grid>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type w:AutoCompleteTextBox}" x:Key="InvestmentTransactionTypeStyle">
            <Setter Property="Watermark" Value="Type"/>
            <Setter Property="ListBoxHeight" Value="150"/>
            <Setter Property="DisplayMember" Value="TypeString"/>
            <Setter Property="IsTextFromItemsSourceOnly" Value="True"/>
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="200"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding TypeString}" Foreground="Blue"/>
                            <TextBlock Text="{Binding Description}" Grid.Column="1" Margin="3,0,0,0"/>
                        </Grid>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style for security symbol autocomplete textbox-->
        <Style TargetType="{x:Type w:AutoCompleteTextBox}" x:Key="InvestmentTransactionSecuritySymbolStyle">
            <Setter Property="Watermark" Value="Symbol"/>
            <Setter Property="ListBoxHeight" Value="150"/>
            <Setter Property="DisplayMember" Value="Symbol"/>
            <Setter Property="IsTextFromItemsSourceOnly" Value="True"/>
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="65"/>
                                <ColumnDefinition Width="360"/>
                                <ColumnDefinition Width="120"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Symbol}"/>
                            <TextBlock Text="{Binding Name}" Grid.Column="1" Margin="3,0"/>
                            <TextBlock Text="{Binding TypeString}" Grid.Column="2" Foreground="Blue"/>
                        </Grid>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Make all ListViewItem occupy the whole horizontal space-->
        <Style TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        </Style>

    </Application.Resources>
</Application>
